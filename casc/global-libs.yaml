unclassified:
  globalLibraries:
    libraries:
      - name: "ci-libs"
        retriever:
          modernSCM:
            scm:
              git:
                remote: "${GITHUB_SHARED_LIBS_REPO}"
                credentialsId: "git-ssh-key"
